# Service principal used for authentication (Depends on Azure Cloud).
client_id = "<add client id (app id) here>"
client_secret = "<add client secret (app secret) here>"
tenant_id = "<add tenan id here>"
subscription_id = "<add subscription here>"
image_version = "1.0.0"
